# WebScrapper

Um projeto de Web Scraping utilizando TypeScript e Node.js.

## 📌 Requisitos

Antes de começar, certifique-se de ter instalado:
- [Node.js](https://nodejs.org/)
- [Yarn](https://yarnpkg.com/)

## 🚀 Instalação

1. Clone este repositório:
   ```sh
   git clone https://github.com/seu-usuario/WebScrapper.git
   cd WebScrapper
   ```

2. Instale as dependências:
   ```sh
   yarn install
   ```

3. Configure o TypeScript:
   ```sh
   yarn tsc --init
   ```

## 🛠 Uso

### Compilar o projeto
```sh
yarn tsc
```

### Executar o projeto
```sh
node dist/index.js
```

### Executar sem compilar (usando `ts-node`)
```sh
yarn ts-node src/index.ts
```

## 📂 Estrutura do Projeto
```
📂 WebScrapper
 ├── 📂 src         # Código-fonte em TypeScript
 │    ├── index.ts  # Arquivo principal
 ├── 📂 dist        # Código compilado em JavaScript
 ├── 📂 node_modules # Dependências (não deve ser enviado para o GitHub)
 ├── .gitignore     # Arquivos ignorados pelo Git
 ├── package.json   # Configurações do projeto e dependências
 ├── tsconfig.json  # Configuração do TypeScript
 ├── yarn.lock      # Versões travadas das dependências
 ├── README.md      # Documentação do projeto
```

## 📋 .gitignore
Adicione ao `.gitignore` para evitar subir arquivos desnecessários:
```
node_modules/
dist/
yarn.lock
```

## 📝 Licença
Este projeto é de uso livre. 🚀

